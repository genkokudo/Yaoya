<Page
    x:Class="Yaoya.Views.MainPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:Yaoya.Properties"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="商品一覧" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
        <Button Grid.Row="0" Content="更新" HorizontalAlignment="Right" Command="{Binding RefrashCommand}" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名前"   Binding="{Binding Name}"     Width="*"/>
                <DataGridTextColumn Header="原産地" Binding="{Binding Origin}"   Width="100"/>
                <DataGridTextColumn Header="価格"   Binding="{Binding Price}"    Width="80"/>
                <DataGridTextColumn Header="種別"   Binding="{Binding Category}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
